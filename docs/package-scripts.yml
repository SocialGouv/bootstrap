scripts:
  build: bundle exec jekyll build
  serve:
    default: concurrently "nps serve.docs" "nps serve.web"
    docs : nps "build -w"
    web : >-
      live-server
      --no-browser
      --mount=/@design.beta.gouv/core:../packages/core
      _site

